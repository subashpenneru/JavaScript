/* let accessFlag;
let age = prompt("How old are you?");

 accessFlag = (age>18)?true:false
console.log(accessFlag); 

accessFlag = (age>3 && age<=15)?"you are not allowed":
             (age>15 && age<18)?"you dont have permission":
             (age>=18)?"allowed":"not"

console.log(accessFlag);
 */

// Assignment Operator 
let x = 10;
x = x^1;
console.log(x);
